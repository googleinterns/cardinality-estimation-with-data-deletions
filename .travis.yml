dist: trusty
sudo: required
language: 
  - cpp
compiler:
  - g++
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++
      - bazel

script:
  bazel build //test:theta_test
